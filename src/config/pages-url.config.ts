class DASHBOARD {
  private root = '/dashboard';
  HOME = this.root;
  TASKS = `${this.root}/tasks`;
  HABITS = `${this.root}/habits`;
  TIMER = `${this.root}/timer`;
  TIME_MANAGEMENT = `${this.root}/time_management`;
  SETTINGS = `${this.root}/settings`;
}
export const DASHBOARD_PAGES = new DASHBOARD();
